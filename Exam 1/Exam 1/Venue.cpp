#include "Event.h"
#include <stdio.h>
#include <iostream>
#include <cstdlib>
#include <string>
#include "Venue.h"
using namespace std;

void Venue::addEvent(int time, string name)// adding an event to the venue
{
		
}
bool Venue::validTime(int time) // checking if time is already used in array
{
	for (int i = 0; i < sizeof scheduleEvents[]; i++)
	{
		if (scheduleEvents[i] == time]
		{

		}
	}
}